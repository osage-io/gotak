# ROSA path: everything runs locally against cluster/service endpoints (no SSH nodes).
[local]
localhost ansible_connection=local

# PATH B (self-managed UPI) would list the 3 nodes here instead:
# [openshift_nodes]
# node1 ansible_host=10.0.1.11
# node2 ansible_host=10.0.2.12
# node3 ansible_host=10.0.3.13
